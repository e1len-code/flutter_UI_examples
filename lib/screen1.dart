import 'package:flutter/material.dart';

class ScreenPage1 extends StatelessWidget {
  const ScreenPage1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ASDASDASDASD')),
    );
  }
}
